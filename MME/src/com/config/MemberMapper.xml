<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">

	        
<insert id="memberAdd"  parameterType="MemberDTO">

	INSERT INTO USER_INFO (
		user_id, 
		user_pw, 
		user_alias, 
		user_name, 
		user_mobile1, 
		user_mobile2, 
		user_mobile3, 
		user_address, 
		user_brand,
		seller_num,
		manager_code
		)
	VALUES (
		#{user_id}, 
		#{user_pw}, 
		#{user_alias}, 
		#{user_name}, 
		#{user_mobile1}, 
		#{user_mobile2}, 
		#{user_mobile3}, 
		#{user_address}, 
		#{user_brand},
		#{seller_num},
		#{manager_code}  
		)

</insert>

<select id="checkUserid" resultType="MemberDTO" parameterType="string">

	SELECT 
		user_id, 
		user_pw, 
		user_alias, 
		user_name, 
		user_mobile1, 
		user_mobile2, 
		user_mobile3, 
		user_address, 
		user_brand, 
		seller_num, 
		manager_code,
		cre_dt
	FROM USER_INFO
	WHERE user_id = #{user_id}

</select>

<select id="checkUseralias" resultType="MemberDTO" parameterType="string">

	SELECT 
		user_id, 
		user_pw, 
		user_alias, 
		user_name, 
		user_mobile1, 
		user_mobile2, 
		user_mobile3, 
		user_address, 
		user_brand, 
		seller_num, 
		manager_code,
		cre_dt
	FROM USER_INFO
	WHERE user_alias = #{user_alias}

</select>


<select id="login" resultType="MemberDTO" parameterType="hashmap">

	SELECT 
		user_id, 
		user_pw, 
		user_alias, 
		user_name, 
		user_mobile1, 
		user_mobile2, 
		user_mobile3, 
		user_address, 
		user_brand, 
		seller_num, 
		manager_code,
		cre_dt
	FROM USER_INFO
	WHERE user_id = #{user_id} and user_pw = #{user_pw}

</select>

	


</mapper>